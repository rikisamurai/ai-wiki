---
title: 派生状态（Derived State）
tags: [react, patterns, performance]
date: 2026-04-22
sources:
  - "[[sources/posts/frontend/React/Blog/Why we banned React's useEffect]]"
last-ingested: 2026-04-22
status: stable
---

派生状态指**从已有 state/props 直接计算出来的值**，不应该用单独的 useState 存储再用 useEffect 同步。这是 [[wiki/frontend/no-useeffect-rule|No useEffect 规则]] 的第一条，也是最高频的违规类型。

> [!example] 反模式：用 Effect 同步派生数据
> ```typescript
> // ❌ BAD: 两次渲染周期 — 先过期，再过滤
> function ProductList() {
>   const [products, setProducts] = useState([]);
>   const [filteredProducts, setFilteredProducts] = useState([]);
>
>   useEffect(() => {
>     setFilteredProducts(products.filter((p) => p.inStock));
>   }, [products]);
> }
>
> // ✅ GOOD: 一次渲染内联计算
> function ProductList() {
>   const [products, setProducts] = useState([]);
>   const filteredProducts = products.filter((p) => p.inStock);
> }
> ```

## 循环陷阱

派生状态用 useEffect 实现的最大风险是**多个 Effect 互相触发**：

```typescript
// ❌ BAD: total 在依赖中可能导致循环
function Cart({ subtotal }) {
  const [tax, setTax] = useState(0);
  const [total, setTotal] = useState(0);

  useEffect(() => { setTax(subtotal * 0.1); }, [subtotal]);
  useEffect(() => { setTotal(subtotal + tax); }, [subtotal, tax, total]);
}

// ✅ GOOD: 无需 Effect
function Cart({ subtotal }) {
  const tax = subtotal * 0.1;
  const total = subtotal + tax;
}
```

## Smell Test

> [!tip] 看到这两种代码立刻警觉
> - 你正要写 `useEffect(() => setX(deriveFromY(y)), [y])`
> - 你有只是镜像其他 state 或 props 的 state

## 性能担忧通常是过早优化

常见反对意见："但内联计算每次渲染都执行一遍，性能不会差吗？"

- 90% 场景下这个开销可以忽略，React 渲染本身比这慢得多
- 真正昂贵的计算应该用 `useMemo`，而不是 useState + useEffect 凑出"懒缓存"
- 多一次渲染（先过期再过滤）的代价反而比内联计算更大

## 关联

- [[wiki/frontend/no-useeffect-rule|No useEffect 规则]] 的第一条
- [[wiki/frontend/usemounteffect|useMountEffect]]：仅在真正需要外部副作用时使用的 Effect 包装
- [[wiki/frontend/key-重置组件|key 重置组件]]：派生失败时的兜底（不要用 Effect 模拟重新挂载）
